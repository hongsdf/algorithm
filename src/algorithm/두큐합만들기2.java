package algorithm;

public class 두큐합만들기2 {
	// 습득한것
	// 두큐의 평균값을 구한다 avg
	
	// 병합정렬처럼 두큐의 인덱스를 만든다.
	// idx1 - 큐1에서 출발, idx2 - 큐2에서 출발  ~ 배열의 *2크기까지 진행가능
	//진행하다 한쪽의 값 이 avg이랑 같아지면 그때 idx + idx2 을 리턴
	
	// worst-case는 두 큐를 전부 바꾸는 것 => 600,000 * 2 = 12,000,000
	// [교차방법]
	// 한쪽이 avg 넘으면 
	// 반대쪽 진행
	
	
	
	
}
